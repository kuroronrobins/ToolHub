launcher:
  name: ToolHub
  default_category: すべて
  logs_dir: "%LOCALAPPDATA%/ToolHub/data/logs"

paths:
  install_dir: "%LOCALAPPDATA%/Programs/ToolHub"
  user_data_dir: "%LOCALAPPDATA%/ToolHub"

updates:
  channel: stable
  manifest_url: https://github.com/kuroronrobins/ToolHub/releases/latest/download/manifest.json
  manifest: release/manifest.json
  app_manifest: release/app_manifest.json
