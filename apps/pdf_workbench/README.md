# PDF Workbench

This app definition was generated by ToolHub App Studio.

## ToolHub Registration

- App ID: `pdf_workbench`
- Version: `0.2.0`
- Build mode: `shared-env`
- Runner: `python_shared_env`
- Entry: `src/main.py`

## Approval

This app starts with `enabled=false` in `release/app_manifest.json`.
Run the approval command after reviewing the generated files and execution test report.

## Notes

- Shared-env uses a versioned ToolHub runtime environment selected from requirements.lock.
- If the dependency version set is new, ToolHub creates a new shared runtime env.
