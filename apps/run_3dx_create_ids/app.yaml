id: run_3dx_create_ids
name: 3DX Create IDs

display:
  icon: icon.png
  icon_fallback: icon.svg
  short_description: "3DX Create IDs をToolHubから起動するアプリです。"
  categories:
    - "業務ツール"

detail:
  description: >
    3DX Create IDs は、指定されたメインファイルをもとにToolHubへ取り込むためのアプリ定義です。
  use_cases:
    - "3DX Create IDs をToolHubからすばやく起動する"
  inputs:
    - "アプリ設定に依存"
  outputs:
    - "アプリ実行結果"
  notes:
    - "正式登録前に実行確認と人間承認が必要です。"

search:
  keywords:
    - 3DX Create IDs
    - run_3dx_create_ids
    - ToolHub
  examples:
    - "3DX Create IDs を起動したい"

run:
  runner: exe
  entry: bin/run_3dx_create_ids/run_3dx_create_ids.exe
  mode: cli

admin:
  version: 0.1.0
  owner: admin
  requirements: requirements.txt
  log_dir: logs

runtime:
  distribution_mode: frozen_folder
  app_env: null
  required_runtime: null
  requirements_lock: requirements.lock

build:
  managed_by: toolhub_app_studio
  build_mode: frozen-folder
  source_entry: C:\Users\kuroron\Downloads\XCgate_AutoUpload-main\XCgate_AutoUpload-main\run_3dx_create_ids.py
  output_mirror: C:\Users\kuroron\Downloads\XCgate_AutoUpload-main\XCgate_AutoUpload-main\ToolHub_AppStudio_Output\run_3dx_create_ids

quality:
  approval_required: true
  execution_test_required: true
