# ToolHub Updater

This directory is reserved for future update metadata and updater integration.

The updater must not write directly over installed files before sha256 verification, compatibility checks, and backup creation are complete.

