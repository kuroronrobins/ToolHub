# ToolHub Installer

This directory is reserved for future installer-specific assets.

The current implementation relies on Tauri bundle outputs collected by `scripts/package_installer.ps1`. Future NSIS, MSI, Inno Setup, or internal deployment definitions should live here.

